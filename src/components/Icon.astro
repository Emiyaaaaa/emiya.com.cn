---
interface Props {
	src: string;
	size: string;
}

const { src, size } = Astro.props;
---

<img
	alt=""
	width={size}
	height={size}
	src={src}
	loading="lazy"
	decoding="async"
	class="inline-block mx-1"
/>
